# Deployment Log

Render auto-deploys `main`. Each row below is one clean deploy. **Rollback-to** is
the prior known-good `main` commit — to undo a deploy, redeploy its Rollback-to SHA
in Render.

| Deploy | Date | Status | Shipped | Source branch | Rollback-to (prior `main`) |
|---|---|---|---|---|---|
| 001 | 2026-05-27 | deployed — PR #123 → `main` `8b1f81b` | Brings `main` up to the dev branch: **M15** dating-narrative guard + `stress-test-findings.md` doc hygiene, on top of the accumulated dating-fix stack (T1a/T1b/T1c, T2a, #6 Phase-1, #111-b termini, M12 …) and the scan-corpus harness + fixtures. The suspected **M14** tufted-armchair dating regression was investigated and proved to be a fixture-faithfulness artifact (stubbed-`p0` skipped negation), **not** an engine regression — branch reproduces main exactly; corpus 32/32 green. | `claude/zen-gauss-91qvM` | `ada0032` |
| 005 | 2026-05-28 | deployed — PR #129 → `main` `8c801f1` | **Data-only checkpoint.** Runtime engine behavior unchanged from Deploy 004 — `engine.ts`, `engineStyleEvaluator.ts`, and `engineCanonicalMap.ts` byte-identical to `origin/main`. Adds the authored `lib/constraints/clueRoutingMap.ts` (525 entries, all six Task B authoring batches + audit pass) as **inert data** — not imported anywhere. `PARKING_LOT.md` adds the **CLUE_ROUTING consumption redesign requirements** for future formal-loop work (multi-clue arbitration, specific-over-generic priority, subtype-aware routing, membership-rule protection, form vs. style as separate layers). No corpus fixtures re-baselined from the rolled-back wiring pass. 126/126 tests pass; typecheck clean. | `claude/zen-gauss-91qvM` | `4c7651a` |
| 004 | 2026-05-28 | deployed — PR #128 → `main` `4c7651a` | **Task A — Cluster B token-architecture fix** (distinctive vs. shared phrase gate in style attribution) closes the Louis XVI / French Provincial conflation across 6 fixtures and unblocks 2 previously-Unresolved cases. Owner authored `distinctive_tokens` + `shared_tokens` per family in `lib/constraints/styleFamilies.ts` (612 lines); engine consumes via new phrase-matching gate in `engineStyleEvaluator.ts` (distinctive hit boosts confidence +0.10/hit capped +0.25; shared-only hits halve confidence; `supported` requires a distinctive hit for families with authored tokens). Backward-compatible — families without authored tokens skip the gate. 18 fixtures re-baselined to current engine output; all are correct attributions surfacing. Two date-range broadenings noted as orthogonal dating-layer concerns (golden_oak china cabinet, porter_balloon_canopy_chair) — not addressed here. Also adds `art_deco_waterfall_vanity` corpus fixture (regression anchor for #15b) and mobile-responsive `ResaleValuationSection` (3-col grid stacks to label/range + full-width note on isMobile ≤640px via existing `useViewport` hook). 126/126 tests pass; typecheck clean. | `claude/zen-gauss-91qvM` | `bc243ed` |
| 003 | 2026-05-28 | deployed — PR #127 → `main` `bc243ed` | Fast-path engine fix **#15b** (sub-finding of #15). Widens the negation detector's gap regex from `(?:\w+\s+){0,3}` to `(?:[\w-]+\s+){0,3}` so hyphenated intervening words like "hand-sawn" don't block legitimate "rather than X" negations. Strictly additive (every previously-matched string still matches). Surfaced by an Art Deco waterfall vanity scan whose `thick_veneer` self-negation ("rather than hand-sawn thick veneer") was missed; the false pre-1910 ceiling on the wood layer had been dropping a c.1935-vintage vanity into a "c. 1860–1910 / reproduction" verdict. Full 39-fixture corpus diff: 38 bit-for-bit unchanged; 1 (`renaissance_revival_tufted_armchair`) had outward verdict identical with internal convergence zones cleaned of a ghost `1960–1970` zone. 125/125 tests pass. Also adds `docs/WAYS_OF_WORKING.md` documenting the default formal loop vs. the fast-path workflow this fix exemplifies. | `claude/zen-gauss-91qvM` | `8deecd5` |
| 002 | 2026-05-28 | deployed — PR #126 → `main` `8deecd5` | First **engine-behavior** deploy since 001: negation hardening (#15/#15a) — the scan corpus now re-derives `negated` exactly as production does (it was consuming stale hand-baked flags), and `descriptionNegatesClue` splits a negated *presence* from a contrasted *subtype* via a quote-rule + affirmation-guard. `swivit`/`sears`/`golden_oak` re-baselined; `art_deco_candelabrum` left KNOWN-RED (#15a: style cues don't anchor dating). Adds the **first CI gate** (`.github/workflows/ci.yml`: typecheck + 125 tests on every push/PR) and promotes the scan corpus to a real test (`tests/corpus.test.ts`, `KNOWN_RED` allowlist). Vercel→Render doc/comment cleanup. (The 39-fixture corpus itself already reached `main` via record-keeping merge #125.) typecheck clean; 125/125 tests; corpus 38/39 green. | `claude/zen-gauss-91qvM` | `f6187fc` |

## How to roll back
1. In Render, open the service → **Deploys**.
2. Redeploy the **Rollback-to** commit for the deploy you want to undo.
3. Record the rollback as a new row (Status: `rollback`, Shipped: `revert NNN`).

## Logging the next deploy
- The resulting `main` SHA of the deploy just shipped becomes the **Rollback-to** of the next one.
- Capture it right after the squash-merge: `git rev-parse origin/main`, then add the new row before merging the next branch.
